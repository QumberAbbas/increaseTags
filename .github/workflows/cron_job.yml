name: Scheduled Job

on:
  schedule:
    - cron: '30 07 * * *'
    - cron: '30 15 * * *'
    - cron: '30 16 * * *'
    - cron: '30 17 * * *'
    - cron: '30 18 * * *'

jobs:
  publish_tag:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout
        uses: actions/checkout@v3

      - name: Say Hello
        id: get_version
        run: echo "Hello! I am Scheduled Job."
